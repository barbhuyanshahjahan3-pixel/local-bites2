{
  "name": "local-bites-web",
  "private": true,
  "version": "1.0.0",
  "description": "Combined build for all Local Bites frontend apps (customer, restaurant, delivery, admin, super-admin)",
  "scripts": {
    "build": "bash build-netlify.sh",
    "build:cloudflare": "bash build-cloudflare.sh"
  }
}
