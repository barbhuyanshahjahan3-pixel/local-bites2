# Dependencies
node_modules/

# Build output
dist/
build/

# Environment files with real secrets — never commit these.
# The .env.example files ARE meant to be committed (they have no secrets).
.env
.env.local
.env.*.local
apps/*/.env
apps/*/.env.local
server/.env

# Editor / OS
.DS_Store
.vscode/
*.log
