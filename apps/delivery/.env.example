VITE_API_URL=http://localhost:5000

# Same public key printed by `npm run generate:vapid` in server/ — needed so
# the browser can subscribe this device for push notifications.
VITE_VAPID_PUBLIC_KEY=
